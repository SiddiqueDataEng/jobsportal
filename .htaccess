RewriteEngine On

# Redirect to index.html if file doesn't exist (for single page app routing)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ index.html [L]
